# OwnNamespace operator model

This document moved to **[allnamespaces.md](allnamespaces.md)**.

The operator is AllNamespaces-only. OwnNamespace is not a supported install
mode (CSV `supported: false`) and must not be required for CatalogSource.
