# COST-7678: Define CostManagement CRD types

**Jira:** [COST-7678](https://redhat.atlassian.net/browse/COST-7678)
**Type:** Story
**Status:** To Do
**Assignee:** Elkana Hendler
**Priority:** Normal
**Created:** 2026-06-10
**Updated:** 2026-07-06
**Parent:** [COST-7470](https://redhat.atlassian.net/browse/COST-7470) — Cost Management on-prem operator

## Description

Deliver the full CRD API surface split across multiple files in api/v1alpha1/:* costmanagement_types.go -- top-level CostManagement, Spec, Status

- infra_types.go -- DatabaseSpec, CacheSpec, KafkaSpec, AuthenticationSpec, ObjectStorageSpec, MonitoringSpec
- app_types.go -- AppSpec, per-component resource overrides, dataRetentionMonths
- status_types.go -- Phase enum (Pending, Discovering, Validating, Migrating, Deploying, Ready, Degraded), all condition types (DiscoveryComplete, StorageReady, DatabaseReady, CacheReady, KafkaReady, AuthenticationReady, SchemaUpToDate, Available, Progressing, Degraded), ComponentStatus, DiscoveredConfig
- profiles.go -- profile enum (standard, ha) and resource sizing maps
- defaults.go -- mutating webhook for defaulting (spec.profile -> standard, spec.infra.monitoring.enabled -> true, spec.app.dataRetentionMonths -> 3)
- validation.go -- webhook + CEL validation rules: required fields (database.host, database.credentialsSecretRef, cache.host, cache.credentialsSecretRef, kafka.bootstrapServers, authentication.issuerUrl), value constraints (database.port 1-65535, database.sslMode one of disable/require/verify-ca/verify-full, authentication.issuerUrl must be HTTPS, authentication.audiences non-empty, profile one of standard/ha, dataRetentionMonths 1-60), cross-field rules (resource requests must not exceed limits)
All infra is external-only (no type field). CR directly accepts connection details (host, port, credentialsSecretRef, bootstrapServers, issuerUrl).

**Scope note (repo):** Profile enum + UI defaults from `spec.profile` landed ([#65](https://github.com/project-koku/koku-service-operator/pull/65)). Shared sizing maps for other workloads are **deferred** (post-beta) → [COST-8095](https://redhat.atlassian.net/browse/COST-8095).


### Out of Scope

- Reconciler logic, resource builders
